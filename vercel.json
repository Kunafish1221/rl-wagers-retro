{
  "version": 2,
  "framework": "nextjs",
  "regions": ["iad1"],
  "crons": [
    {
      "path": "/api/system/matches/expire",
      "schedule": "*/2 * * * *",
      "headers": {
        "authorization": "Bearer ${SYSTEM_CRON_SECRET}"
      }
    }
  ]
}